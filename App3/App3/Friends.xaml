<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App3.Friends"
             Title="Friends">
    <ContentPage.Content>
        <RefreshView x:Name="refreshView" Refreshing="refreshView_Refreshing" RefreshColor="White">

            <ScrollView BackgroundColor="Black" VerticalOptions="Fill">
                <StackLayout HorizontalOptions="Fill">
                    <StackLayout Orientation="Horizontal" Spacing="0" Padding="0" HorizontalOptions="StartAndExpand">
                        <Label Text="Friends" Padding="20,20,20,20" TextColor="White" FontSize="50" HeightRequest="70"/>
                        <Button Text="Scan" TextColor="White" Clicked="btnScan_Clicked" HorizontalOptions="EndAndExpand"/>
                    </StackLayout>
                    <ListView x:Name="friendsListView" RowHeight="80" BackgroundColor="Black"></ListView>
                </StackLayout>
            </ScrollView>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>