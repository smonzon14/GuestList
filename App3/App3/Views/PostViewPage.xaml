<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App3.Views.PostViewPage"
             x:Name="postView"
             ControlTemplate="{StaticResource header}">
    <ContentPage.Content>
        <RefreshView RefreshColor="White" Refreshing="RefreshView_Refreshing" >
            <ListView BackgroundColor="#1f1f1f" x:Name="commentsListView" HasUnevenRows="True" SeparatorColor="#1f1f1f">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid BackgroundColor="#2f2f2f">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="5"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="5"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="5"/>
                                </Grid.RowDefinitions>
                                <Frame Grid.Column="1" Grid.Row="1" WidthRequest="30" HeightRequest="30" CornerRadius="15" HorizontalOptions="EndAndExpand" VerticalOptions="StartAndExpand" Padding="0" IsClippedToBounds="True">
                                    <Image HorizontalOptions="Center" VerticalOptions="Center" Source="Profile"/>
                                </Frame>
                                <StackLayout Grid.Column="2" Grid.Row="1" Orientation="Horizontal" BackgroundColor="Transparent" Padding="10,0,10,0" Spacing="5">
                                    <StackLayout Padding="0" Orientation="Vertical" HorizontalOptions="StartAndExpand">
                                        <Label Text="{Binding name}" FontSize="14" TextColor="Gray"/>
                                        <Label Text="{Binding message}" FontSize="16" HorizontalOptions="StartAndExpand" TextColor="White" LineBreakMode="WordWrap" Padding="0,0,20,0"/>
                                    </StackLayout>
                                </StackLayout>
                                <StackLayout Spacing="5" Orientation="Vertical" Grid.Column="3" Grid.Row="1">
                                    <ImageButton Source="button_like" Padding="0" WidthRequest="20" />
                                    <Label Text="{Binding likes}" FontSize="12" TextColor="White" Padding="0" HorizontalOptions="Center" VerticalOptions="Center"/>
                                </StackLayout>

                            </Grid>
                        </ViewCell>
                    </DataTemplate>

                </ListView.ItemTemplate>
                <ListView.Header>
                    <StackLayout Spacing="0" BindingContext="{Binding Post}">

                        <Grid  BackgroundColor="#292929">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="5"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="5"/>
                            </Grid.RowDefinitions>
                            <Frame Grid.Column="1" Grid.Row="1" WidthRequest="40" HeightRequest="40" CornerRadius="20" HorizontalOptions="EndAndExpand" VerticalOptions="StartAndExpand" Padding="0" IsClippedToBounds="True">
                                <Image HorizontalOptions="Center" VerticalOptions="Center" Source="Profile"/>
                            </Frame>
                            <StackLayout Grid.Column="2" Grid.Row="1" Orientation="Vertical" Padding="10,0,10,0" Spacing="5" HorizontalOptions="FillAndExpand">
                                <StackLayout Padding="0" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                                    <Label Text="{Binding name}" FontSize="14" TextColor="Gray"/>
                                    <ImageButton Source="button_dots" Padding="0" WidthRequest="20" HorizontalOptions="EndAndExpand" BackgroundColor="Transparent"/>
                                </StackLayout>

                                <Label Text="{Binding message}" FontSize="16" HorizontalOptions="StartAndExpand" TextColor="White" LineBreakMode="WordWrap" Padding="0,0,20,0"/>
                                <StackLayout Orientation="Horizontal" HorizontalOptions="Fill" BackgroundColor="Transparent" Spacing="20">
                                    <StackLayout Spacing="5" Orientation="Horizontal">
                                        <ImageButton Source="{Binding Liked, Converter={StaticResource likeToButtonSource}}" Padding="0" WidthRequest="20" Command="{Binding LikeCommand}" CommandParameter="{Binding .}"/>
                                        <Label Text="{Binding likes}" FontSize="14" TextColor="White" Padding="0" HorizontalOptions="Center" VerticalOptions="Center" />
                                    </StackLayout>
                                    <StackLayout Spacing="5" Padding="0" Orientation="Horizontal">
                                        <ImageButton Source="button_comment_simple" Padding="0" WidthRequest="20" IsEnabled="False"/>
                                        <Label Text="{Binding comments}" FontSize="14" TextColor="White" Padding="0" HorizontalOptions="Center" VerticalOptions="Center"/>
                                    </StackLayout>
                                </StackLayout>


                            </StackLayout>


                        </Grid>
                        <ProgressBar x:Name="progressBar" ProgressColor="Aqua" Progress="0"/>
                        <StackLayout Orientation="Horizontal" Padding="15">

                            <Frame CornerRadius="15" Padding="5" HasShadow="False" BackgroundColor="#101010" HorizontalOptions="FillAndExpand" >

                                <Editor x:Name="commentEditor" MaxLength="200" BackgroundColor="Transparent" Placeholder="Comment" TextColor="White" FontSize="16" AutoSize="TextChanges" PlaceholderColor="Gray" WidthRequest="310"/>

                            </Frame>
                            <StackLayout Orientation="Vertical" Spacing="0" WidthRequest="100" HorizontalOptions="FillAndExpand">
                                <Button Text="Send" Clicked="Button_Clicked" HorizontalOptions="End" TextColor="#F50058"/>
                                <Label Text="{Binding Text.Length, Source={x:Reference commentEditor}, FallbackValue='0/200', StringFormat='{0}/200'}" TextColor="White" HorizontalOptions="EndAndExpand" FontSize="14" HorizontalTextAlignment="Center"/>
                            </StackLayout>

                        </StackLayout>
                    </StackLayout>

                </ListView.Header>
            </ListView>
        </RefreshView>
        
            
        
    </ContentPage.Content>
</ContentPage>