<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App3.HomePage"
             xmlns:magic="clr-namespace:MagicGradients;assembly=MagicGradients" xmlns:models="clr-namespace:App3.Models"
             x:Name="homePage"
             ControlTemplate="{StaticResource hiddenBarTemplate}">
    <ContentPage.Content>

        <!--<RefreshView x:Name="refreshView" RefreshColor="Turquoise" Refreshing="refresh">-->

        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="170"/>
                <RowDefinition Height="0" x:Name="keyboardSpacing"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>

            <models:PartyMap x:Name="map" AbsoluteLayout.LayoutFlags="All" Grid.RowSpan="6" Grid.ColumnSpan="2"/>
            <!--<Label Text="ZP" TextColor="White" FontSize="30" FontAttributes="Italic,Bold"  Grid.Column="2" HorizontalTextAlignment="Center" Grid.Row="1" Padding="15"/>-->
            <StackLayout Padding="70,5,70,0" HorizontalOptions="FillAndExpand" VerticalOptions="Start" Grid.Row="1" Grid.ColumnSpan="2">
                <Frame Padding="0" CornerRadius="15" BackgroundColor="Transparent">
                    <SearchBar x:Name="searchBar" IsVisible="false" BackgroundColor="#d0ffffff" Placeholder="Search Invites" PlaceholderColor="#F50058" FontSize="14" TextColor="White" HorizontalTextAlignment="Center" HorizontalOptions="FillAndExpand" Focused="SearchBar_Focused" Unfocused="SearchBar_Unfocused" HeightRequest="30" TextChanged="searchBar_TextChanged"/>

                </Frame>
            </StackLayout>

            <StackLayout x:Name="controls" Grid.Row="3" Grid.Column="1" VerticalOptions="EndAndExpand" Padding="10">
                <Frame Padding="0" BackgroundColor="#0b6bd9" HorizontalOptions="Center" VerticalOptions="Center" CornerRadius="20">

                    <ImageButton Source="tab_search" Clicked="SearchButton_Clicked" Padding="10" WidthRequest="40" HeightRequest="40"/>
                </Frame>
            </StackLayout>

            <StackLayout Grid.Row="4" Grid.ColumnSpan="2">
                <CarouselView x:Name="partyCarousel" VerticalOptions="EndAndExpand" BackgroundColor="#00000000" Opacity="1.0" PeekAreaInsets="40" HeightRequest="200">
                    <CarouselView.EmptyView>
                        <Label Text="No Invites" FontAttributes="Bold" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" TextColor="White" BackgroundColor="#57000000" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                    </CarouselView.EmptyView>
                    <CarouselView.ItemTemplate>
                        <DataTemplate>

                            <StackLayout>
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                                </StackLayout.GestureRecognizers>
                                <Frame CornerRadius="20" Padding="0" WidthRequest="320" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" BackgroundColor="#d0d0d0">
                                    <AbsoluteLayout>

                                        <StackLayout Spacing="5" Padding="12" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">

                                            <Label Text="{Binding name}" FontSize="18" TextColor="Black" FontAttributes="Bold" HorizontalOptions="Center"/>
                                            <Label Text="{Binding thrower}" FontSize="12" TextColor="Gray"/>
                                            <Label Text="{Binding address}" FontSize="14" TextColor="Black" LineBreakMode="TailTruncation" MaxLines="1"/>
                                            <Label Text="{Binding time}" FontSize="14" TextColor="Black"/>
                                            <!--, StringFormat='{0:d} @ {0:h:mm tt}'-->
                                        </StackLayout>
                                    </AbsoluteLayout>

                                </Frame>

                            </StackLayout>


                        </DataTemplate>

                    </CarouselView.ItemTemplate>
                </CarouselView>

            </StackLayout>




        </Grid>

        <!--</RefreshView>-->

    </ContentPage.Content>
</ContentPage>