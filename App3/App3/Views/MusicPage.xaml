<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:magic="clr-namespace:MagicGradients;assembly=MagicGradients"
             x:Class="App3.Views.MusicPage"
             >
    <ContentPage.Content>
        <StackLayout  BackgroundColor="Black" Padding="40">
            <ListView x:Name="genreListView" VerticalOptions="FillAndExpand" ItemTapped="genreListView_ItemTapped" BackgroundColor="Black" SeparatorVisibility="None" IsPullToRefreshEnabled="True" Refreshing="ListView_Refreshing" RefreshControlColor="White">
                <ListView.Header>
                    <Label Text="My Music Genres" HorizontalTextAlignment="Start" TextColor="White" FontSize="30" Padding="10" FontAttributes="Bold"/>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell Height="50">
                            <StackLayout Orientation="Horizontal" Spacing="20">
                                <CheckBox Color="#F50058" IsChecked="{Binding Liked, Mode=TwoWay}"/>
                                <Label Text="{Binding Genre}" TextColor="White"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Footer>
                    <StackLayout Padding="30" VerticalOptions="EndAndExpand" Grid.ColumnSpan="4" Grid.Row="9">
                        <Frame CornerRadius="30" Padding="0" BackgroundColor="Transparent">
                            <Grid>
                                <magic:GradientView VerticalOptions="FillAndExpand">
                                    <magic:GradientView.GradientSource >
                                        <magic:CssGradientSource>
                                            <x:String>
                                                <![CDATA[
            linear-gradient(to right, #F50058, #D90B80)
                    ]]>
                                            </x:String>
                                        </magic:CssGradientSource>
                                    </magic:GradientView.GradientSource>
                                </magic:GradientView>
                                <Button Text="Save" HeightRequest="60" BackgroundColor="Transparent" TextColor="White" FontSize="30" Clicked="onSubmit"/>
                            </Grid>

                        </Frame>

                    </StackLayout>
                </ListView.Footer>
            </ListView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>