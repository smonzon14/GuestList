<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App3.PartyPostPreviewPage">
    <ContentPage.Content>
        
        <Grid BackgroundColor="Gray" x:Name="mainGrid" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition x:Name="partyRow" Height="220"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <!-- Main Party Stack -->
            <StackLayout x:Name="partySearchStack" Grid.Row="1" Spacing="0">

                <!--<ScrollView BackgroundColor="#C9000000" VerticalOptions="Fill" HeightRequest="260">
                -->
                <CarouselView x:Name="partyListView" BackgroundColor="#00000000" Opacity="1.0" PeekAreaInsets="50"></CarouselView>
                <!--
                </ScrollView>
                -->
            </StackLayout>
            <Button Text="Post and Invite" Grid.Row="2" Clicked="postButtonClicked" BackgroundColor="Aquamarine" TextColor="Black" Padding="30,0,30,0"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>