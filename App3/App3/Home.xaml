<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App3.Home"
             Title="Home"
             xmlns:views="clr-namespace:App3"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             xmlns:custommap="clr-namespace:App3.Maps">
    <ContentPage.ToolbarItems>

        <ToolbarItem Text="Settings" Clicked="OnSettingsButtonClicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        
        <RefreshView x:Name="refreshView" RefreshColor="Turquoise" Refreshing="refresh" >
            
            <StackLayout BackgroundColor="Black" x:Name="mainStack">

                
                <ScrollView BackgroundColor="Black" VerticalOptions="Fill">
                    
                    <StackLayout>
                        <Label HeightRequest="60" Text="Parties 🥳" FontSize="40" TextColor="White" Padding="20,0,0,0"/>


                        <ListView x:Name="partyListView" RowHeight="170" BackgroundColor="Black" Opacity="50"></ListView>
                        
                    </StackLayout>
                    
                </ScrollView>
            </StackLayout>
        </RefreshView>

    </ContentPage.Content>
</ContentPage>